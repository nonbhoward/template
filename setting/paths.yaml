# a list of roots that will be populate the 'files' key in paths dict,
#   to prevent population of a specific root, remove it from this list
enabled roots:
  [home, project, projects, root, run, script]

# operating system specific, user-defined paths
#   roots can be used to start a path
#   TODO not implemented
additional paths:
  win32:
    bash aliases: [C:\, Program Files, Git, etc, profile.d, aliases.sh]
